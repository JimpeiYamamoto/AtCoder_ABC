#include <bits/stdc++.h>
using namespace std;
#define rep(i, N) for (int i = 0; i < N; i++)
typedef long long ll;
using Graph = vector< vector<int> >;
vector<bool> seen;

int	main()
{
	int	N;
	string	S;
	cin >> N;
	cin >> S;
	int	i = 0;
	while (i < N)
	{
		i++;
	}
	if (S[i - 1] == 'o')
		cout << "Yes" << endl;
	else
		cout << "No" << endl;
}
